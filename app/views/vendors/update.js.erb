<% unless @vendor.errors.empty? %>
  $('#errors').html('<%= errors_for @vendor %>');
<% else %>
  $('#vendor_<%= @vendor.id %>').replaceWith("<%= escape_javascript(render @vendor) %>");
  $('#vendor_<%= @vendor.id %> .show').addClass('edited')
<% end %>
